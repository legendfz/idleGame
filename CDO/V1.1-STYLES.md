# V1.1-STYLES.md — v1.1 可用 CSS 代码

**版本**：v1.1
**日期**：2026-03-01
**作者**：CDO
**用途**：CTO 可直接复制到 `src/index.css` 或拆分为模块

---

## 1. 六级品质配色 CSS

### 1.1 CSS 变量

```css
/* ── 品质色彩 Token ── */
:root {
  /* 主色 */
  --q-common:   #b0b0b0;
  --q-good:     #43a047;
  --q-rare:     #1e88e5;
  --q-epic:     #8e24aa;
  --q-legend:   #ef6c00;
  --q-hongmeng: #c62828;

  /* 暗色主题衬色（行背景） */
  --q-common-bg:   rgba(176,176,176, 0.04);
  --q-good-bg:     rgba(67,160,71,   0.05);
  --q-rare-bg:     rgba(30,136,229,  0.05);
  --q-epic-bg:     rgba(142,36,170,  0.07);
  --q-legend-bg:   rgba(239,108,0,   0.08);
  --q-hongmeng-bg: rgba(198,40,40,   0.10);
}
```

### 1.2 品质 class（用于物品名、列表行等）

```css
/* ── 品质文字 ── */
.q-common   { color: var(--q-common); }
.q-good     { color: var(--q-good); }
.q-rare     { color: var(--q-rare); }
.q-epic     { color: var(--q-epic);   font-weight: 600; }
.q-legend   { color: var(--q-legend); font-weight: 600; text-shadow: 0 0 6px rgba(239,108,0, 0.4); }
.q-hongmeng { color: var(--q-hongmeng); font-weight: 700; text-shadow: 0 0 8px rgba(198,40,40, 0.5); }

/* ── 品质行背景（用于背包列表行） ── */
.q-row-common   { background: var(--q-common-bg);   border-left: 3px solid var(--q-common); }
.q-row-good     { background: var(--q-good-bg);     border-left: 3px solid var(--q-good); }
.q-row-rare     { background: var(--q-rare-bg);     border-left: 3px solid var(--q-rare); }
.q-row-epic     { background: var(--q-epic-bg);     border-left: 3px solid var(--q-epic); }
.q-row-legend   { background: var(--q-legend-bg);   border-left: 3px solid var(--q-legend); }
.q-row-hongmeng { background: var(--q-hongmeng-bg); border-left: 3px solid var(--q-hongmeng); }

/* ── 品质弹窗边框 ── */
.modal-q-common   { border-color: var(--q-common); }
.modal-q-good     { border-color: var(--q-good); }
.modal-q-rare     { border-color: var(--q-rare); }
.modal-q-epic     { border-color: var(--q-epic); border-width: 2px; }
.modal-q-legend   { border-color: var(--q-legend); border-width: 2px; }
.modal-q-hongmeng { border-color: var(--q-hongmeng); border-width: 2px; }
```

### 1.3 品质标签条（弹窗顶部）

```css
.quality-badge {
  display: inline-block;
  padding: 2px 12px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: bold;
  letter-spacing: 2px;
  margin-bottom: 8px;
}
.quality-badge.legend {
  background: var(--q-legend);
  color: #fff;
}
.quality-badge.hongmeng {
  background: var(--q-hongmeng);
  color: #fff;
  animation: badge-pulse-hongmeng 1.5s ease-in-out infinite;
}
@keyframes badge-pulse-hongmeng {
  0%, 100% { opacity: 1; }
  50%      { opacity: 0.7; }
}
```

### 1.4 品质掉落特效

```css
/* ── 极品：屏幕边缘紫色闪光 ── */
@keyframes edge-glow-epic {
  0%   { box-shadow: inset 0 0 0 0 rgba(142,36,170, 0); }
  40%  { box-shadow: inset 0 0 40px 8px rgba(142,36,170, 0.25); }
  100% { box-shadow: inset 0 0 0 0 rgba(142,36,170, 0); }
}
.glow-epic { animation: edge-glow-epic 500ms ease-out; }

/* ── 仙品/鸿蒙：全屏闪屏 ── */
.screen-flash {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 280;
  opacity: 0;
}
.screen-flash.legend {
  background: rgba(239,108,0, 0.15);
  animation: flash-in 400ms ease-out forwards;
}
.screen-flash.hongmeng {
  background: rgba(198,40,40, 0.2);
  animation: flash-in 600ms ease-out forwards;
}
@keyframes flash-in {
  0%   { opacity: 0; }
  25%  { opacity: 1; }
  100% { opacity: 0; }
}

/* ── 鸿蒙 Toast 边框脉动 ── */
@keyframes border-pulse-hongmeng {
  0%, 100% { border-color: #c62828; box-shadow: 0 0 12px rgba(198,40,40, 0.3); }
  50%      { border-color: #f0c040; box-shadow: 0 0 12px rgba(240,192,64, 0.3); }
}
.toast-hongmeng {
  animation: border-pulse-hongmeng 800ms ease-in-out infinite;
  border-width: 2px;
}

/* ── 鸿蒙标题逐字出现 ── */
@keyframes char-reveal {
  from { opacity: 0; transform: translateY(4px); }
  to   { opacity: 1; transform: translateY(0); }
}
.hongmeng-title span {
  display: inline-block;
  opacity: 0;
  animation: char-reveal 200ms ease forwards;
}
/* JS 中给每个 <span> 设置 animation-delay: i * 120ms */

/* ── 上品 slide-in ── */
@keyframes slide-in-right {
  from { transform: translateX(30px); opacity: 0; }
  to   { transform: translateX(0); opacity: 1; }
}
.drop-slide-in { animation: slide-in-right 300ms ease-out; }
```

### 1.5 TypeScript 品质映射（供 CTO 参考）

```ts
export type Quality = 'common' | 'good' | 'rare' | 'epic' | 'legend' | 'hongmeng';

export const QUALITY_INFO: Record<Quality, {
  label: string;
  prefix: string;
  cssClass: string;
  rowClass: string;
  multiplier: number;
  color: string;
}> = {
  common:   { label: '凡品', prefix: '○', cssClass: 'q-common',   rowClass: 'q-row-common',   multiplier: 1,  color: '#b0b0b0' },
  good:     { label: '良品', prefix: '●', cssClass: 'q-good',     rowClass: 'q-row-good',     multiplier: 2,  color: '#43a047' },
  rare:     { label: '上品', prefix: '◆', cssClass: 'q-rare',     rowClass: 'q-row-rare',     multiplier: 4,  color: '#1e88e5' },
  epic:     { label: '极品', prefix: '★', cssClass: 'q-epic',     rowClass: 'q-row-epic',     multiplier: 8,  color: '#8e24aa' },
  legend:   { label: '仙品', prefix: '✦', cssClass: 'q-legend',   rowClass: 'q-row-legend',   multiplier: 16, color: '#ef6c00' },
  hongmeng: { label: '鸿蒙', prefix: '✧', cssClass: 'q-hongmeng', rowClass: 'q-row-hongmeng', multiplier: 30, color: '#c62828' },
};
```

---

## 2. 离线收益展示弹窗样式

### 2.1 弹窗结构 CSS

```css
/* ── 离线收益弹窗 ── */
.offline-modal {
  background: var(--panel);
  border: 2px solid var(--accent);
  border-radius: 12px;
  padding: 0;
  max-width: 320px;
  width: 90%;
  overflow: hidden;
  animation: modal-pop 300ms cubic-bezier(0.34, 1.56, 0.64, 1);
}

@keyframes modal-pop {
  from { transform: scale(0.9); opacity: 0; }
  to   { transform: scale(1);   opacity: 1; }
}

/* 关闭动画 class（JS 在点击领取时添加） */
.offline-modal.closing {
  animation: modal-close 300ms ease-out forwards;
}
@keyframes modal-close {
  to { transform: scale(0.95); opacity: 0; }
}

/* ── 标题区 ── */
.offline-header {
  text-align: center;
  padding: var(--space-lg) var(--space-lg) var(--space-md);
}
.offline-header h2 {
  color: var(--accent);
  font-size: 18px;
  margin-bottom: 4px;
}
.offline-header .duration {
  font-size: 13px;
  color: var(--text-dim);
}

/* ── 收益区块 ── */
.offline-section {
  margin: 0 var(--space-md) var(--space-sm);
  background: var(--bg);
  border-radius: 8px;
  padding: var(--space-sm) var(--space-md);
}
.offline-section-title {
  font-size: 11px;
  color: var(--text-dim);
  margin-bottom: var(--space-xs);
  border-bottom: 1px solid var(--border);
  padding-bottom: var(--space-xs);
}

/* ── 收益行 ── */
.offline-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 3px 0;
  font-size: 14px;
}
.offline-row .label {
  color: var(--text);
}
.offline-row .value {
  color: var(--accent);
  font-family: var(--font-mono, monospace);
  font-weight: bold;
}

/* ── 装备掉落行 ── */
.offline-drop-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 3px 0;
  font-size: 13px;
  animation: slide-in-right 300ms ease-out both;
}
/* JS: animation-delay = index * 200ms */

/* ── 战斗成果区动画 ── */
.offline-battle-stats {
  animation: fade-slide-up 400ms ease-out both;
  animation-delay: 1200ms;
}
@keyframes fade-slide-up {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* ── 领取按钮 ── */
.offline-claim-btn {
  display: block;
  width: calc(100% - var(--space-lg) * 2);
  margin: var(--space-md) auto var(--space-lg);
  background: var(--accent);
  color: var(--bg);
  border: none;
  padding: 14px 0;
  border-radius: 8px;
  font-size: 16px;
  font-weight: bold;
  cursor: pointer;
  transition: transform 0.1s;
  animation: fade-in 300ms ease both;
  animation-delay: 2200ms;
  opacity: 0;
}
@keyframes fade-in {
  to { opacity: 1; }
}
.offline-claim-btn:active {
  transform: scale(0.96);
}

/* ── 24h 封顶提示 ── */
.offline-cap-notice {
  text-align: center;
  font-size: 11px;
  color: var(--text-dim);
  margin: 0 var(--space-md) var(--space-sm);
}
```

### 2.2 JSX 结构参考

```tsx
<div className="modal-overlay">
  <div className="offline-modal">
    {/* 标题 */}
    <div className="offline-header">
      <h2>🌙 离线收益</h2>
      <div className="duration">⏱️ 离开了 {hours}小时 {mins}分</div>
    </div>

    {/* 基础收益 */}
    <div className="offline-section">
      <div className="offline-section-title">基础收益</div>
      <div className="offline-row">
        <span className="label">🪙 灵石</span>
        <span className="value">+{animatedLingshi}</span>
      </div>
      <div className="offline-row">
        <span className="label">✨ 经验</span>
        <span className="value">+{animatedExp}</span>
      </div>
      {pantao > 0 && (
        <div className="offline-row">
          <span className="label">🍑 蟠桃</span>
          <span className="value">+{pantao}</span>
        </div>
      )}
    </div>

    {/* 战斗成果（>5min 时显示） */}
    {duration > 300 && (
      <div className="offline-section offline-battle-stats">
        <div className="offline-section-title">战斗成果</div>
        <div className="offline-row">
          <span className="label">⚔️ 击败怪物</span>
          <span className="value">×{mobKills}</span>
        </div>
        <div className="offline-row">
          <span className="label">🐉 击败Boss</span>
          <span className="value">×{bossKills}</span>
        </div>
        {levelGained > 0 && (
          <div className="offline-row">
            <span className="label">📈 等级</span>
            <span className="value" style={{color: 'var(--green)'}}>
              {oldLevel} → {newLevel}
            </span>
          </div>
        )}
      </div>
    )}

    {/* 装备掉落 */}
    {drops.length > 0 && (
      <div className="offline-section">
        <div className="offline-section-title">装备掉落</div>
        {drops.map((item, i) => (
          <div key={i} className="offline-drop-item"
            style={{animationDelay: `${1800 + i * 200}ms`}}>
            <span className={QUALITY_INFO[item.quality].cssClass}>
              {QUALITY_INFO[item.quality].prefix}{item.name}
            </span>
            <span style={{color: 'var(--text-dim)', fontSize: 12}}>
              {item.slot === 'weapon' ? '⚡' : '❤️'}+{getEquipEffectiveStat(item)}
            </span>
          </div>
        ))}
      </div>
    )}

    {/* 24h 封顶提示 */}
    {duration >= 86400 && (
      <div className="offline-cap-notice">最多计算 24 小时离线收益</div>
    )}

    {/* 领取按钮 */}
    <button className="offline-claim-btn" onClick={handleClaim}>
      ✅ 全部领取
    </button>
  </div>
</div>
```

### 2.3 countUp 动画（JS 工具函数）

```ts
function useCountUp(target: number, duration = 800, delay = 300): number {
  const [value, setValue] = useState(0);
  useEffect(() => {
    const timer = setTimeout(() => {
      const start = performance.now();
      const step = (now: number) => {
        const elapsed = now - start;
        const progress = Math.min(elapsed / duration, 1);
        // easeOutQuad
        const eased = 1 - (1 - progress) * (1 - progress);
        setValue(Math.floor(target * eased));
        if (progress < 1) requestAnimationFrame(step);
      };
      requestAnimationFrame(step);
    }, delay);
    return () => clearTimeout(timer);
  }, [target, duration, delay]);
  return value;
}
```

---

## 3. 反馈入口样式

### 3.1 更多页面菜单项

```css
/* ── 更多页菜单列表 ── */
.menu-list {
  background: var(--panel);
  border-radius: 8px;
  overflow: hidden;
}
.menu-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px var(--space-md);
  border-bottom: 1px solid var(--border);
  cursor: pointer;
  font-size: 14px;
  transition: background 0.1s;
  -webkit-user-select: none;
  user-select: none;
}
.menu-item:last-child { border-bottom: none; }
.menu-item:active { background: rgba(240,192,64, 0.06); }
.menu-item .arrow { color: var(--text-dim); font-size: 12px; }
.menu-separator {
  height: 1px;
  background: var(--border);
  margin: 0;
}
```

### 3.2 反馈类型选择器

```css
/* ── 反馈类型按钮组 ── */
.feedback-types {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-sm);
  margin-bottom: var(--space-md);
}
.feedback-type-btn {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 12px;
  text-align: center;
  font-size: 13px;
  color: var(--text);
  cursor: pointer;
  transition: all 0.15s;
  -webkit-user-select: none;
  user-select: none;
}
.feedback-type-btn .icon {
  display: block;
  font-size: 20px;
  margin-bottom: 4px;
}
.feedback-type-btn:active { transform: scale(0.97); }
.feedback-type-btn.selected {
  background: var(--accent);
  color: var(--bg);
  border-color: var(--accent);
  font-weight: bold;
}
```

### 3.3 文本输入区

```css
/* ── 反馈文本框 ── */
.feedback-textarea {
  width: 100%;
  min-height: 120px;
  max-height: 200px;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 8px;
  color: var(--text);
  padding: var(--space-md);
  font-family: system-ui, -apple-system, sans-serif;
  font-size: 16px; /* ≥16px 防 iOS zoom */
  line-height: 1.5;
  resize: none;
  -webkit-appearance: none;
}
.feedback-textarea::placeholder {
  color: var(--text-dim);
}
.feedback-textarea:focus {
  border-color: var(--accent);
  outline: none;
  box-shadow: 0 0 0 2px rgba(240,192,64, 0.15);
}
.feedback-char-count {
  text-align: right;
  font-size: 11px;
  color: var(--text-dim);
  margin-top: var(--space-xs);
}
.feedback-char-count.over {
  color: var(--red);
}
```

### 3.4 自动附加信息区

```css
/* ── 附加信息卡片 ── */
.feedback-context {
  background: var(--bg);
  border-radius: 8px;
  padding: var(--space-sm) var(--space-md);
  margin: var(--space-md) 0;
  font-size: 12px;
  color: var(--text-dim);
  line-height: 1.8;
}
.feedback-context .ctx-title {
  font-size: 11px;
  color: var(--text-dim);
  margin-bottom: var(--space-xs);
}
```

### 3.5 提交按钮

```css
/* ── 提交按钮 ── */
.feedback-submit {
  width: 100%;
  background: var(--accent);
  color: var(--bg);
  border: none;
  padding: 14px 0;
  border-radius: 8px;
  font-size: 15px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.15s;
  margin-top: var(--space-md);
}
.feedback-submit:active { transform: scale(0.97); }
.feedback-submit:disabled {
  background: var(--border);
  color: var(--text-dim);
  cursor: default;
  transform: none;
}

/* ── 提交状态 ── */
.feedback-submit.loading {
  pointer-events: none;
  opacity: 0.7;
}
.feedback-submit.loading::after {
  content: '…';
  animation: dots 1s steps(3) infinite;
}
@keyframes dots {
  0%   { content: '.'; }
  33%  { content: '..'; }
  66%  { content: '...'; }
}
```

### 3.6 提交结果 Toast

```css
/* ── 提交成功/失败 Toast ── */
.feedback-toast {
  position: fixed;
  top: 30%;
  left: 50%;
  transform: translateX(-50%) scale(0.9);
  background: var(--panel);
  border: 1px solid var(--accent);
  border-radius: 12px;
  padding: var(--space-lg);
  text-align: center;
  max-width: 280px;
  width: 85%;
  z-index: 300;
  animation: toast-in 300ms cubic-bezier(0.34,1.56,0.64,1) forwards;
}
@keyframes toast-in {
  to { transform: translateX(-50%) scale(1); }
}
.feedback-toast.success { border-color: var(--green); }
.feedback-toast.error   { border-color: var(--red); }
.feedback-toast h3 { margin-bottom: var(--space-sm); }
.feedback-toast .auto-close {
  font-size: 11px;
  color: var(--text-dim);
  margin-top: var(--space-sm);
}
```

### 3.7 返回按钮（子页面顶栏）

```css
/* ── 子页面顶栏（替代常规 TopBar） ── */
.sub-page-header {
  display: flex;
  align-items: center;
  padding: var(--space-sm) var(--space-md);
  padding-top: calc(var(--space-sm) + env(safe-area-inset-top));
  background: var(--panel);
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}
.sub-page-header .back-btn {
  background: none;
  border: none;
  color: var(--accent);
  font-size: 16px;
  cursor: pointer;
  padding: 8px;
  margin-left: -8px;
  min-width: 44px;
  min-height: 44px;
  display: flex;
  align-items: center;
}
.sub-page-header .title {
  flex: 1;
  text-align: center;
  font-size: 15px;
  color: var(--text);
  font-weight: 600;
  margin-right: 44px; /* 平衡返回按钮宽度 */
}
```
